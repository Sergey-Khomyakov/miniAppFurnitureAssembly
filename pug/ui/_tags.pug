mixin tags(items)
    div(class="flex gap-1")
        each item, index in items
            div(class="flex items-center justify-center pt-1 pr-4 pl-4 pb-2 rounded-full bg-slate-200")
                p(class="font-montserrat font-semibold text-sm text-[#929292]") #{item.name}